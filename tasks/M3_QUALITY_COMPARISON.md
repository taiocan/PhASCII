# Task M3 — Quality Comparison Modes

## Goal

Generate alternative outputs so the user can choose which transformation is more recognizable.

## Required modes

- `stylized` baseline.
- `dithered-experimental` alternative.

## Deferred

- Edge detection remains deferred unless the strategic agent authorizes it.

## Acceptance criteria

- CLI can generate both modes.
- Each mode creates `.txt` and `.png` outputs.
- Add a quality report under `reports/quality/` using `QUALITY_RUBRIC.md`.
